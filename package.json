{
  "name": "csvtosql",
  "version": "1.0.0",
  "main": "csvtosql.js",
  "description": "convert csv to sqlite",
  "scripts": {
    "lint": "npx eslint csvtosql.js",
    "test": "tap --no-coverage --reporter=specy -J --color",
    "test:coverage": "npm test -- --coverage-report=html",
    "build:node": "esbuild csvtosql.js --bundle --minify --platform=node --target=node10.4 --outfile=build/csvtosql.min.js",
    "build:binary": "pkg package.json",
	"build": "npm run build:node && npm run build:binary",
    "dump:sqlite": "sqlite3 example/sample1.db -init example/sample1.sql"
  },
  "keywords": ["csv", "sqlite"],
  "author": "hawyar",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.32.0",
    "tap": "^15.0.9"
  },
  "dependencies": {
    "esbuild": "^0.13.1",
    "split2": "^3.2.2"
  },
   "pkg": {
    "outputPath": "build/binary"
  },
  "bin": "build/csvtosql.min.js"
}
